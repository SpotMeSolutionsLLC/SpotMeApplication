const INITIAL_STATE = {
  location: '',
  currentLocation: {},
  inputData: '',
  predictions: {},
  sanjose: {},
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return {...state}
  }
};
